workflow:
   rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: always
    - when: never

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - docker build -t furni-decor-api:$CI_COMMIT_SHORT_SHA .

deploy:
  stage: deploy
  script:
    - echo "sha commit:$CI_COMMIT_SHORT_SHA"
